# 탑티어 완성도 기준 (Quality Standards)

> **이 파일은 Claude Code가 본 프로젝트의 모든 기능 개발·수정 시 반드시 준수해야 하는 품질 지침입니다.**
> 작업 완료 전 아래 체크리스트를 반드시 통과한 후 커밋하십시오.

마지막 갱신: 2026-05-23

---

## 1. 핵심 원칙

**"탑티어 어필리에이트 그룹 운영 수준"** 이란:

- 실제 유저(베팅 이용자)가 돈을 쓰기 전 **참고할 만한 정보**를 제공
- 신뢰할 수 있는 **실데이터 기반** (추측/할루시네이션 절대 금지)
- 경쟁 채널과 차별화되는 **전문성** (단순 복붙이 아닌 인사이트)
- **일관성** — 매 포스트, 매 기능이 동일한 품질 기준을 유지

---

## 2. 스포츠 파이프라인 완성도 기준

### 2.1 콘텐츠 품질 체크리스트 (포스트당)

- [ ] **실데이터만 사용** — 순위/폼/득실차/배당은 반드시 API에서 받은 값
- [ ] **배당 섹션** — Odds API 키 있을 때 반드시 1X2 + Over/Under 표시
- [ ] **H2H 포함** — 두 팀 직접 대결 최근 3경기 (Football-Data.org `/matches?homeTeam=X&awayTeam=Y`)
- [ ] **픽 복기 연동** — 리뷰 포스트는 pick_history에서 이전 픽 조회 후 적중 여부 명시
- [ ] **적중률 푸터** — pick_history 실데이터 기반 이번 달 적중률 표시
- [ ] **CTA 버튼** — 모든 포스트에 어필리에이트 링크 포함
- [ ] **해시태그** — 리그명·팀명·스포츠픽·오늘의픽 포함

### 2.2 콘텐츠 타입 체크리스트 (파이프라인당)

- [ ] **경기 프리뷰** — 킥오프 최소 2시간 전 게시 (순위+폼+배당+픽)
- [ ] **경기 리뷰** — 경기 종료 2시간 내 게시 (결과+픽 복기+다음 경기 포인트)
- [ ] **주간 픽 성적표** — 매주 월요일 09:00 KST (이번 주 전적 요약)
- [ ] **월간 적중률 리포트** — 매월 1일 (pick_history 전체 집계)
- [ ] **리그 순위 업데이트** — 매주 수요일 (피크 라운드 전날)
- [ ] **득점왕 레이스** — 각 리그 Top 5 스코러 (월 2회)

### 2.3 포스팅 빈도 기준

| 시간대 | KST | 내용 |
|--------|-----|------|
| 04:00 UTC | 13:00 KST | 당일 경기 프리뷰 (최대 4건) |
| 10:00 UTC | 19:00 KST | 결과 리뷰 + 다음날 프리뷰 (최대 4건) |
| 킥오프 3시간 전 | — | 매치 스케줄러 자동 프리뷰 |
| 킥오프 110분 후 | — | 매치 스케줄러 자동 리뷰 |
| 월요일 09:00 KST | 00:00 UTC | 주간 픽 성적표 |

---

## 3. 코드 품질 기준

### 3.1 함수/모듈 작성 규칙

- **파일 당 300줄 하드 한도** — 200줄 초과 시 분리 검토, 300줄 초과 시 즉시 분리
- **타입 힌팅 강제** — 모든 함수에 파라미터 타입 + 반환 타입 명시
- **실패 허용 설계** — 외부 API 실패 시 graceful fallback, 절대 파이프라인 전체 중단 금지
- **로깅 수준 준수** — `logger.info`(정상), `logger.warning`(복구 가능), `logger.exception`(치명적)

### 3.2 외부 API 사용 원칙

- **Football-Data.org** — 10 calls/min 준수, sleep_sec=7.0 (프로덕션), 1.0 (디버그)
- **The Odds API** — 500 req/month 절약 위해 `odds_cache` 재사용 필수, 중복 호출 금지
- **Anthropic API** — 프롬프트에 "검증된 실제 데이터" 블록 주입, 할루시네이션 방지 지시 필수
- **모든 API** — `hours_ahead=168` (7일) 기준으로 미리 패치, 당일 만료 리스크 제거

### 3.3 DB 사용 원칙

- **UNIQUE 제약** — 중복 방지 필드에는 반드시 UNIQUE INDEX + ON CONFLICT DO NOTHING
- **pick_history** — record_pick / record_result / get_pick_for_match 세 함수 페어로만 접근
- **channel_content** — source 필드는 `api:sports:{league_name}` 형식 일관성 유지

---

## 4. 코드 수정 전 체크리스트

```
[ ] 외부 API 응답이 빈 값일 때의 fallback 처리가 있는가?
[ ] 새 함수에 타입 힌팅이 있는가?
[ ] 파일 라인 수가 300을 초과하지 않는가?
[ ] DB 삽입 함수에 ON CONFLICT 처리가 있는가?
[ ] 프로덕션 sleep_sec이 6.0 이상인가? (FD API rate limit)
[ ] 배당/순위/폼 데이터가 AI에게 "이 데이터만 사용하라"고 명시적으로 지시되었는가?
[ ] 새 콘텐츠 타입이 source_prefix 체계와 호환되는가?
```

---

## 5. 작업 완료 정의 (Definition of Done)

어떤 기능이든 아래를 모두 만족해야 "완료"로 간주합니다:

1. **실데이터 검증** — debug 엔드포인트에서 실제 API 데이터 포함 확인
2. **end-to-end 테스트** — 채널/그룹 실제 게시 확인 (dev 아닌 prod 환경)
3. **엣지케이스 처리** — API 실패·데이터 없음·시즌 외 기간 모두 graceful
4. **로그 확인** — Railway 로그에서 WARNING/ERROR 없음 확인
5. **문서 갱신** — DEVLOG.md + TODO.md + memory 파일 업데이트

---

## 6. 스포츠 파이프라인 금지 사항

- ❌ AI가 생성한 스코어·순위·선수 이름을 검증 없이 포스팅
- ❌ 배당 섹션 없는 픽 포스트 (The Odds API 키 있을 때)
- ❌ pick_history 연동 없는 리뷰 포스트 (픽 복기 없음)
- ❌ 하루 4건 미만 포스팅 (스포츠 전용 게시판 기준)
- ❌ upcoming 정렬 없는 경기 선택 (가장 임박한 경기 우선)
- ❌ 시즌 종료 기간 콘텐츠 공백 (CL/WC/K리그로 롤오버)
